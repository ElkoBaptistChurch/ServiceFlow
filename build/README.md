This directory is `electron-builder`'s `buildResources` directory (see `electron-builder.yml`).

No icon file is committed here — add `build/icon.ico` manually when church branding is
ready. `electron-builder` works without it in the meantime; it falls back to Electron's
default icon.
